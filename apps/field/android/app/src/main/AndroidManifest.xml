<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        INTERNET e a UNICA permissao do aplicativo.

        O Field fala com a API do AlfaOS e com mais nada. Camera, localizacao,
        armazenamento e telefone NAO sao pedidos: fotos, GPS e evidencias
        entram em fases futuras, e discar e navegar acontecem por Intent
        (`tel:`, Maps, Waze), que sao apps externos e nao exigem permissao.

        Pedir permissao "para depois" treina o usuario a aceitar sem ler, e no
        dia em que a camera for realmente necessaria o consentimento ja terá
        perdido o significado.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="AlfaOS Field"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!--
        Consultas de pacote que o aplicativo precisa fazer.

        No Android 11+ `canLaunchUrl` devolve false para um esquema que nao
        esteja declarado aqui, mesmo com o app instalado. Sem estas entradas os
        botoes de ligar e navegar apareceriam desabilitados num aparelho que
        tem discador, Google Maps e Waze — que e o caso de qualquer celular de
        tecnico.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <!-- Waze usa esquema proprio. -->
        <package android:name="com.waze"/>
        <package android:name="com.google.android.apps.maps"/>
    </queries>
</manifest>
